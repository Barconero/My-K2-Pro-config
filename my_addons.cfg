###########################################
# My set of Macros V15
# Created 29/05/2026 
###########################################
# LINE_PURGE - for use in Start G-code
# NOZZLE_LED_ON
# NOZZLE_LED_OFF
# MY_UNLOAD_FILAMENT
# MY_LOAD_FILAMENT
# ABS_PREPARE
# SCREWS_CALIBRATION
# MY_ADAPTIVE - removed because of Orca 2.4.0
# WARMUP
# TEST_RESONANCES_GRAPHS
# BELTS_SHAPER_CALIBRATION
# MY_PID
# CAL_XY / CAL_E1 / CAL_E2
# GIT_CFG
###########################################

[virtual_pins]

[respond]

[save_variables]  
filename: /mnt/UDISK/printer_data/config/variables.cfg

###########################################
# ADAPTIVE Purge Line Macros
[output_pin Adaptive_PURGE_Line]
pin: virtual_pin:Adaptive_PURGE_Line_pin
value: 1

[delayed_gcode RESTORE_PURGE_MODE]
initial_duration: 2.5
gcode:
    {% set saved_state = printer.save_variables.variables.purge_mode | default(1) | int %}
    SET_PIN PIN=Adaptive_PURGE_Line VALUE={saved_state}
    UPDATE_DELAYED_GCODE ID=SYNC_PURGE_LOOP DURATION=4

[delayed_gcode SYNC_PURGE_LOOP]
gcode:
    {% set current_pin = printer['output_pin Adaptive_PURGE_Line'].value | int %}
    {% set saved_state = printer.save_variables.variables.purge_mode | default(1) | int %}
    {% if current_pin != saved_state %}
        SAVE_VARIABLE VARIABLE=purge_mode VALUE={current_pin}
    {% endif %}
    UPDATE_DELAYED_GCODE ID=SYNC_PURGE_LOOP DURATION=4

[gcode_macro DRAW_PRIME_LINE]
description: Classic prime line
gcode:
   M204 S2000
   G90 ## XYZ in absolute mode
   G1 Z3 F600
   M83
   G1 Y70 F12000
   G1 X0 F12000
   G1 Z0.2 F600
   G1 X0 Y70 F6000
   G1 E0.8 F300
   G1 X0 Y0 E7 F5000
   G1 X70 Y0 E7 F5000
   G92 E0
   G1 Z1 F600

[gcode_macro MY_PURGE]
description: A purge macro that adapts to be near your actual printed objects
gcode:
    # Purge settings
    {% set purge_height = 0.8 | float %}            # Z position of nozzle during purge. Default is 0.8.
    {% set tip_distance = 0 | float %}              # Distance between tip of filament and nozzle before purge. Default is 0.
    {% set flow_rate    = 12 | float %}             # Flow rate of purge in mm3/s. Default is 12.
    {% set purge_amount = 25 | float %}             # Amount of filament to be purged prior to printing. Default is 50.
    {% set purge_margin = 12 | float %}             # Distance the purge will be in front of the print area. Default is 10.

    # Get relevant printer params
    {% set travel_speed = (printer.toolhead.max_velocity) * 60 | float %}
    {% set cross_section = printer.configfile.settings.extruder.max_extrude_cross_section | float %}

    # Use firmware retraction if it is defined
    {% if printer.firmware_retraction is defined %}
        {% set RETRACT = G10 | string %}
    {% else %}
        {% set RETRACT = 'G1 E-0.6 F2400' | string %}
    {% endif %}

    {% set bed_x_max = printer["gcode_macro PRINTER_PARAM"].max_x_position | float %}
    {% set bed_y_max = printer["gcode_macro PRINTER_PARAM"].max_y_position | float %}

    # Rapid move distance to break string
    {% set rapid_move = 10 %}

    # Calculate purge origins and centers from objects
    {% set all_points = printer.exclude_object.objects | map(attribute='polygon') | sum(start=[]) %}    # Get all object points
    {% set purge_x_min = (all_points | map(attribute=0) | min | default(0)) %}                          # Object x min
    {% set purge_x_max = (all_points | map(attribute=0) | max | default(0)) %}                          # Object x max
    {% set purge_y_min = (all_points | map(attribute=1) | min | default(0)) %}                          # Object y min
    {% set purge_y_max = (all_points | map(attribute=1) | max | default(0)) %}                          # Object y max

    {% set detect_object = purge_x_min + purge_x_max + purge_y_min + purge_y_max %}                     # Object size > 0

    {% set purge_x_center = ([((purge_x_max + purge_x_min) / 2) - (purge_amount / 2), 0] | max) %}      # Start point of purge line relative to print on X axis
    {% set purge_y_center = ([((purge_y_max + purge_y_min) / 2) - (purge_amount / 2), 0] | max) %}      # Start point of purge line relative to print on Y axis

    {% if (purge_x_center + purge_amount + rapid_move) > bed_x_max %}
        {% set purge_x_center = (bed_x_max - (purge_amount + rapid_move)) %}
    {% endif %}
    {% if (purge_y_center + purge_amount + rapid_move) > bed_y_max %}
        {% set purge_y_center = (bed_y_max - (purge_amount + rapid_move)) %}
    {% endif %}

    {% set purge_x_origin_low = (purge_x_min - purge_margin) %}
    {% set purge_x_origin_high = (purge_x_max + purge_margin) %}
    {% set purge_y_origin_low = (purge_y_min - purge_margin) %}
    {% set purge_y_origin_high = (purge_y_max + purge_margin) %}

    {% set purge_move_speed = (flow_rate / 5.0) * 60 | float %}

    {% if cross_section < 5 %}
        RESPOND TYPE=command MSG="[Extruder] max_extrude_cross_section is insufficient for line purge, please set it to 5 or greater. Purge skipped."
    {% else %}
        SAVE_GCODE_STATE NAME=Prepurge_State
        {% if detect_object == 0 %}
            RESPOND TYPE=command MSG="No object detected! Using classic purge line."
            DRAW_PRIME_LINE
            {RETRACT}
        {% elif purge_y_origin_low > 0 %}
            RESPOND TYPE=command MSG="Purge line starting at {purge_x_center}, {purge_y_origin_low} and purging {purge_amount}mm of filament, requested flow rate is {flow_rate}mm3/s."
            G92 E0
            G0 F{travel_speed}
            G90
            G0 X{purge_x_center} Y{purge_y_origin_low}
            G0 Z{purge_height}
            M83
            G1 E{tip_distance} F{purge_move_speed}
            G1 X{purge_x_center + purge_amount} E{purge_amount} F{purge_move_speed}
            {RETRACT}
            G0 X{purge_x_center + purge_amount + rapid_move} F{travel_speed}         
            G92 E0
            G0 Z{purge_height * 2} F{travel_speed}
        {% elif purge_x_origin_low > 0 %}
            RESPOND TYPE=command MSG="Purge line starting at {purge_x_origin_low}, {purge_y_center} and purging {purge_amount}mm of filament, requested flow rate is {flow_rate}mm3/s."
            G92 E0
            G0 F{travel_speed}
            G90
            G0 X{purge_x_origin_low} Y{purge_y_center}
            G0 Z{purge_height}
            M83
            G1 E{tip_distance} F{purge_move_speed}
            G1 Y{purge_y_center + purge_amount} E{purge_amount} F{purge_move_speed}
            {RETRACT}
            G0 Y{purge_y_center + purge_amount + rapid_move} F{travel_speed}
            G92 E0
            G0 Z{purge_height * 2} F{travel_speed}
        {% elif purge_y_origin_high < bed_y_max %}
            RESPOND TYPE=command MSG="Purge line starting at {purge_x_center}, {purge_y_origin_high} and purging {purge_amount}mm of filament, requested flow rate is {flow_rate}mm3/s."
            G92 E0
            G0 F{travel_speed}
            G90
            G0 X{purge_x_center} Y{purge_y_origin_high}
            G0 Z{purge_height}
            M83
            G1 E{tip_distance} F{purge_move_speed}
            G1 X{purge_x_center + purge_amount} E{purge_amount} F{purge_move_speed}
            {RETRACT}
            G0 X{purge_x_center + purge_amount + rapid_move} F{travel_speed}
            G92 E0
            G0 Z{purge_height * 2} F{travel_speed}
        {% elif purge_x_origin_high < bed_x_max %}
            RESPOND TYPE=command MSG="Purge line starting at {purge_x_origin_high}, {purge_y_center} and purging {purge_amount}mm of filament, requested flow rate is {flow_rate}mm3/s."
            G92 E0
            G0 F{travel_speed}
            G90
            G0 X{purge_x_origin_high} Y{purge_y_center}
            G0 Z{purge_height}
            M83
            G1 E{tip_distance} F{purge_move_speed}
            G1 Y{purge_y_center + purge_amount} E{purge_amount} F{purge_move_speed}
            {RETRACT}
            G0 Y{purge_y_center + purge_amount + rapid_move} F{travel_speed}
            G92 E0
            G0 Z{purge_height * 2} F{travel_speed}
        {% else %}
            RESPOND TYPE=command MSG="No space for purge line! Using classic purge line."
            DRAW_PRIME_LINE
            {RETRACT}
        {% endif %}
        RESTORE_GCODE_STATE NAME=Prepurge_State
    {% endif %}

[gcode_macro LINE_PURGE]
description: Slicer Start G-code PURGE Line
gcode:
  {% if printer['output_pin Adaptive_PURGE_Line'].value == 1 %}
    RESPOND TYPE=command MSG="Starting Adaptive Purge Line..."
    MY_PURGE
  {% else %}  
    RESPOND TYPE=command MSG="Starting Classic Prime Line..."
    DRAW_PRIME_LINE
  {% endif %}


###########################################
# NOZZLE_LED Macros
# https://github.com/TheIncompetentCraftsman/K2-Plus-Nozzle-LED/blob/main/led%20macros
[gcode_macro NOZZLE_LED_ON]
gcode:
  load_ai_nozzle_cam_power_on

[gcode_macro NOZZLE_LED_OFF]
gcode:
  load_ai_nozzle_cam_power_off


###########################################
# FILAMENT Macros
[gcode_macro MY_UNLOAD_FILAMENT]
gcode:
  {% set TEMP = params.TEMP|default(230)|int %}
  SAVE_GCODE_STATE NAME=unload_state
  M109 S{TEMP}
  RESPOND TYPE=command MSG="Unloading filament..."
  M83
  G1 E10 F300
  G1 E-15 F3000
  G1 E-22.4700 F2400
  G1 E-6.4200 F1200
  G1 E-3.2100 F720
  G1 E5.0000 F356
  G1 E-5.0000 F384
  G1 E5.0000 F412
  G1 E-5.0000 F440
  G1 E5.0000 F467
  G1 E-5.0000 F495
  G1 E5.0000 F523
  G1 E-5.0000 F3000
  G1 E-90 F3000
  M400
  RESTORE_GCODE_STATE NAME=unload_state

[gcode_macro MY_LOAD_FILAMENT]
gcode:
  {% set TEMP = params.TEMP|default(230)|int %}
  SAVE_GCODE_STATE NAME=load_state
  M109 S{TEMP}
  RESPOND TYPE=command MSG="Loading filament..."
  M83
  G1 E140 F180
  M400
  RESTORE_GCODE_STATE NAME=load_state
  

###########################################
# ABS_PREPARE Macros
[gcode_macro ABS_PREPARE]
description: Prepares printer for ABS printing
gcode:
  SET_TEMPERATURE_FAN_TARGET TEMPERATURE_FAN=chamber_fan TARGET=58
  SET_HEATER_TEMPERATURE HEATER=chamber_heater TARGET=55
  SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=90


###########################################
# SCREWS_CALIBRATION Macros
[screws_tilt_adjust]
screw1: 35, 35
screw1_name: front left
screw2: 275, 35
screw2_name: front right
screw3: 275, 275
screw3_name: rear right
screw4: 35, 275
screw4_name: rear left
horizontal_move_z: 5
speed: 300
screw_thread: CW-M4

[gcode_macro SCREWS_CALIBRATION]
description: For screws manual iterative calibration
gcode:
    {% set temp = params.TEMP|default(70)|float %} ; wait for bed to hit 50C (PLA temp), 70C PETG, 90C ABS
    G28
    M190 S{temp}      
    M109 S150         ; nozzle warm but not oozing
    G28 Z             ; re-home Z at temp
    SCREWS_TILT_CALCULATE
    TURN_OFF_HEATERS


###########################################
# ADAPTIVE BED MESH CALIBRATE Macros
#
#[output_pin Adaptive_BED_Mesh]
#pin: virtual_pin:Adaptive_BED_Mesh_pin
#value: 0

#[delayed_gcode RESTORE_MESH_MODE]
#initial_duration: 2.5
#gcode:
#    {% set saved_state = printer.save_variables.variables.mesh_mode | default(0) | int %}
#    SET_PIN PIN=Adaptive_BED_Mesh VALUE={saved_state}
#    UPDATE_DELAYED_GCODE ID=SYNC_MESH_LOOP DURATION=3

#[delayed_gcode SYNC_MESH_LOOP]
#gcode:
#    {% set current_pin = printer['output_pin Adaptive_BED_Mesh'].value | int %}
#    {% set saved_state = printer.save_variables.variables.mesh_mode | default(0) | int %}
#    {% if current_pin != saved_state %}
#        SAVE_VARIABLE VARIABLE=mesh_mode VALUE={current_pin}
#    {% endif %}
#    UPDATE_DELAYED_GCODE ID=SYNC_MESH_LOOP DURATION=3

#[gcode_macro MY_ADAPTIVE]
#description: Slicer Start G-code Adaptive BED Leveling
#gcode:
#  {% if printer['output_pin Adaptive_BED_Mesh'].value == 1 %}
#    RESPOND TYPE=command MSG="Starting Adaptive BED Leveling..."
#    {% if 'FILENAME' in params|upper %}
#      {% set filename = params.FILENAME %}
#    {%else %}
#      {% set filename = printer.print_stats.filename %}
#    {% endif %}
#    BED_MESH_CALIBRATE_START_PRINT GCODE_FILE={filename}
#  {% else %}  
#    RESPOND TYPE=command MSG="Bypass BED Leveling..."
#  {% endif %}


###########################################
# WARMUP (movement stress test — adopted from K1)
[gcode_macro WARMUP]
description: Movement warm-up to seat bearings and rods. Usage: WARMUP LOOPS=10 ACCEL=5000
gcode:
  {% set loops = params.LOOPS|default(10)|int %}
  {% set accel = params.ACCEL|default(5000)|int %}

  {% set start_x = 5 | int %}
  {% set start_y = 5 | int %}
  {% set end_x = (printer["gcode_macro PRINTER_PARAM"].max_x_position -5) %}
  {% set end_y = (printer["gcode_macro PRINTER_PARAM"].max_y_position -5) %}

  {action_respond_info("Starting warmup: %d loops at %d mm/s² acceleration" % (loops, accel))}
  RESPOND TYPE=command MSG="This moves the toolhead across the full bed. Make sure it is clear."

  {% if printer.toolhead.homed_axes != "xyz" %}
    G28
  {% endif %}

  ; Save current acceleration
  SAVE_GCODE_STATE NAME=warmup_state

  SET_VELOCITY_LIMIT ACCEL={accel}

  G90
  G1 F18000 # 300 mm/s

  {% for i in range(loops) %}
    G1 X{start_x} Y{start_y}
    G1 X{end_x}   Y{start_y}
    G1 X{end_x}   Y{end_y}
    G1 X{start_x} Y{end_y}
    G1 X{start_x} Y{start_y}
    G1 X{end_x}   Y{end_y}
    G1 X{start_x} Y{end_y}
    G1 X{end_x}   Y{start_y}
  {% endfor %}

  G1 X{(end_x / 2)|int} Y{(end_y / 2)|int}

  ; Restore original acceleration
  RESTORE_GCODE_STATE NAME=warmup_state

  RESPOND TYPE=command MSG="Warmup complete. {loops|int} loops done."


###########################################
# Generate raw CSV data for external analysis 
#[gcode_shell_command clean_res_data]
#command: sh -c "rm -f /tmp/raw_data_resonance_*.csv"
#timeout: 5.0
#verbose: False

#[gcode_shell_command graph_x]
#command: sh -c "python3 /usr/share/klipper/scripts/calibrate_shaper.py /tmp/raw_data_resonance_x_*.csv -o /mnt/UDISK/printer_data/config/resonance_x.png"
#timeout: 60.0
#verbose: True

#[gcode_shell_command graph_y]
#command: sh -c "python3 /usr/share/klipper/scripts/calibrate_shaper.py /tmp/raw_data_resonance_y_*.csv -o /mnt/UDISK/printer_data/config/resonance_y.png"
#timeout: 60.0
#verbose: True

[gcode_shell_command move_csv_files]
# Moves resonance CSV files from /tmp/ to the config directory
command: sh -c "mv /tmp/raw_data_*.csv /mnt/UDISK/printer_data/config/"
timeout: 5.0
verbose: True

[gcode_macro MOVE_CSV]
description: Moves resonance CSV files from /tmp to the config directory
gcode:
  RESPOND TYPE=command MSG="Execute CVS file transfer"
  RUN_SHELL_COMMAND CMD=move_csv_files
  RESPOND TYPE=command MSG="Done..."
  RESPOND TYPE=command MSG="CSV files saved to /mnt/UDISK/printer_data/config/"

[gcode_macro TEST_RESONANCES_GRAPHS]
description: Generate raw resonance CSV data for both axes for external analysis
gcode:
  {% set enabled = printer["filament_switch_sensor filament_sensor"].enabled %}
  {% if enabled %}
    SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=0
  {% endif %}

  {% if printer.toolhead.homed_axes != "xyz" %}
    G28
  {% endif %}

  M400

#  RUN_SHELL_COMMAND CMD=clean_res_data

  {action_respond_info("====================")}
  RESPOND TYPE=command MSG="Generating resonance data for X axis..."
  TEST_RESONANCES AXIS=X OUTPUT=raw_data NAME=resonance_x
  M400

#  RESPOND TYPE=command MSG="Drawing X axis graph. Please wait (~10-20 seconds)..."
#  RUN_SHELL_COMMAND CMD=graph_x

  RESPOND TYPE=command MSG="Generating resonance data for Y axis..."

  TEST_RESONANCES AXIS=Y OUTPUT=raw_data NAME=resonance_y
  M400

#  RESPOND TYPE=command MSG="Drawing Y axis graph. Please wait..."
#  RUN_SHELL_COMMAND CMD=graph_y

  {% if enabled %}
    SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=1
  {% endif %}

  RESPOND TYPE=command MSG="===================="
  RESPOND TYPE=command MSG="Done. CSV files saved to /mnt/UDISK/printer_data/config/"


###########################################
# Belt tension analysis 
#[gcode_shell_command clean_belts_data]
#command: sh -c "rm -f /tmp/raw_data_belt_*.csv"
#timeout: 5.0
#verbose: False

#[gcode_shell_command graph_belts]
#command: sh -c "python3 /usr/share/klipper/scripts/graph_accelerometer.py -c /tmp/raw_data_belt_a_*.csv /tmp/raw_data_belt_b_*.csv -o /mnt/UDISK/printer_data/config/belts_comparison.png"
#timeout: 90.0
#verbose: True

[gcode_macro BELTS_SHAPER_CALIBRATION]
description: Analyse belt tension via frequency sweep. Compare A and B profiles.
gcode:
  {% set min_freq     = params.MIN_FREQ|default(5.0) | float %}
  {% set max_freq     = params.MAX_FREQ|default(80.0) | float %}
  {% set hz_per_sec   = params.HZ_PER_SEC|default(1.0) | float %}
  {% set accel_per_hz = params.ACCEL_PER_HZ|default(200.0) | float %}

  # Disable filament sensor during tests
  {% set enabled = printer["filament_switch_sensor filament_sensor"].enabled %}
  {% if enabled %}
    SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=0
  {% endif %}

  {% if printer.toolhead.homed_axes != "xyz" %}
    G28
  {% endif %}

  M400

#  RUN_SHELL_COMMAND CMD=clean_belts_data

  RESPOND TYPE=command MSG="Starting belt tension analysis..."
  RESPOND TYPE=command MSG="Min freq: {min_freq|int}Hz  Max freq: {max_freq|int}Hz"

  RESPOND TYPE=command MSG="Testing Belt A..."
  TEST_RESONANCES AXIS=1,-1 OUTPUT=raw_data NAME=belt_a FREQ_START={min_freq} FREQ_END={max_freq} HZ_PER_SEC={hz_per_sec} ACCEL_PER_HZ={accel_per_hz}
  M400

  RESPOND TYPE=command MSG="Testing Belt B..."
  TEST_RESONANCES AXIS=1,1 OUTPUT=raw_data NAME=belt_b FREQ_START={min_freq} FREQ_END={max_freq} HZ_PER_SEC={hz_per_sec} ACCEL_PER_HZ={accel_per_hz}
  M400

  RESPOND TYPE=command MSG="===================="

#  RESPOND TYPE=command MSG="Generating combined graph. Please wait (~40-90 seconds)..."
#  RUN_SHELL_COMMAND CMD=graph_belts

  # Restore filament sensor state
  {% if enabled %}
    SET_FILAMENT_SENSOR SENSOR=filament_sensor ENABLE=1
  {% endif %}

  RESPOND TYPE=command MSG="Belt analysis complete."
##  RESPOND TYPE=command MSG="Graph saved to /mnt/UDISK/printer_data/config/belts_comparison.png"


###########################################
# Calibration of both PIDs
[gcode_macro MY_PID]
description: Calibration of both nozzle PIDs by Porco Rosso
gcode:
  {% set l_temp = params.LOW_TEMP|default(245)|float %}
  {% set h_temp = params.HIGH_TEMP|default(310)|float %} 

  G28

  SET_PIN PIN=fan0 VALUE=255
  SET_PIN PIN=fan2 VALUE=255

  RESPOND TYPE=command MSG="Starting hotend low PID calibration at {l_temp|int}°C..."
  PID_CALIBRATE HEATER=extruder TARGET={l_temp}
  CXSAVE_CONFIG

  {% set b_temp = printer.configfile.config['extruder'].get('high_temp_value', 9999) | float %}

  {% if h_temp > b_temp %}
      RESPOND TYPE=command MSG="Starting hotend high PID calibration at {h_temp|int}°C..."
      PID_CALIBRATE HEATER=extruder TARGET={h_temp}
      CXSAVE_CONFIG
  {% else %}
      RESPOND TYPE=error MSG="High temp is too low for calibration ({h_temp|int}°C vs {b_temp|int}°C)."
  {% endif %}

  SET_PIN PIN=fan2 VALUE=0
  WAIT_TEMP_START
  M106 P0 S127 F1

  RESPOND TYPE=command MSG="Hotend PIDs calibration done."
  M84

###########################################
# Motors Calibration 
[gcode_macro CAL_XY]
description: Calibration XY motors
gcode:
   RESPOND MSG="Move the nozzle to the center"
   G28

   RESPOND MSG="Motors switched to disabled state"
   M84

   RESPOND TYPE=error MSG="Wait for about 3 minutes. Do not power off the machine during calibration, as it may cause abnormalities"
   MOTOR_FLASH_PARAM NUM=1 DATA=6 PARAMS_TYPE=float ID=9
   MOTOR_FLASH_PARAM NUM=1 DATA=6 PARAMS_TYPE=float ID=25
   MOTOR_FLASH_PARAM NUM=2 DATA=6 PARAMS_TYPE=float ID=9
   MOTOR_FLASH_PARAM NUM=2 DATA=6 PARAMS_TYPE=float ID=25
   MOTOR_ELEC_OFFSET_CALIBRATE NUM=1
   MOTOR_ELEC_OFFSET_CALIBRATE NUM=2
   RESPOND MSG="Done !!!"

[gcode_macro CAL_E1]
description: Calibration extruder encoder 
gcode:
   MOTOR_ENCODER_CALIBRATE_OFFICIAL NUM=1 EXTRUDER=True
   RESPOND TYPE=error MSG="Restart the printer. And run CAL_E2 macro

[gcode_macro CAL_E2]
description: Calibration extruder zero position
gcode:
   MOTOR_ELEC_OFFSET_CALIBRATE NUM=1 EXTRUDER=True
   RESPOND MSG="Done !!!"


###########################################
# Git_cfg
[gcode_shell_command backup_to_github]
command: python3 /mnt/UDISK/printer_data/config/backup_git.py
timeout: 60.0
verbose: True

[gcode_macro GIT_CFG]
description: Send cfg's to GitHub via API
gcode:
    RESPOND MSG="Send to GitHub..."
    RUN_SHELL_COMMAND CMD=backup_to_github
    RESPOND MSG="Done !!!"

